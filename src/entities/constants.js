/**
 * constants.js
 * Shared numeric constants for the entities layer.
 */

/** X offset (px) from a defender sprite origin to the gun muzzle. */
export const BULLET_SPAWN_OFFSET_X = 22;

/** Delay (ms) after a trail emitter stops following before it is destroyed. */
export const TRAIL_CLEANUP_DELAY_MS = 260;

/** How long (ms) before an un-hit bullet is auto-retired. */
export const BULLET_LIFETIME_MS = 2000;
