/**
 * UIScene.js
 * Blank UI overlay — Phase 1 placeholder.
 */
export default class UIScene extends Phaser.Scene {
  constructor() {
    super({ key: 'UIScene' });
  }

  create() {
    // Phase 1: intentionally empty
  }
}
